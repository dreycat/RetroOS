export enum StatusGame {
  Win = 'win',
  Fail = 'fail',
  Start = 'start',
  Init = 'init'
}

export enum Cell {
  Wall = 0,
  Path = 1,
  Key = 'K',
  Door = 'D',
  Portal = 'P',
  Box = 'B',
  Gold = 'G'
}
